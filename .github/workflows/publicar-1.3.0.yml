name: Canal de actualizaciones

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  informacion:
    runs-on: ubuntu-latest
    steps:
      - name: Estado
        run: echo "El canal público de actualizaciones de Las Ninfas Gestión está preparado."
